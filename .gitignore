# Your own usage snapshots and fitted caps — personal telemetry, never commit
history.jsonl
calibration.json

# Your account list — local paths, and which logins you happen to have
settings.json

# Virtual environment (created by uv)
.venv/

__pycache__/
*.pyc
